import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import type { TelemetryPoint } from '../store';
export function TrendChart({ data }: { data: TelemetryPoint[] }) { const points = data.slice(-72).map(p => ({ ...p, time: new Date(p.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) })); return <div className="h-64 w-full">{points.length < 2 ? <div className="flex h-full items-center justify-center text-sm text-slate-500">Đang chờ đủ dữ liệu realtime…</div> : <ResponsiveContainer><LineChart data={points}><XAxis dataKey="time" stroke="#64748b" minTickGap={32} /><YAxis stroke="#64748b" width={42} /><Tooltip contentStyle={{ background: '#0f172a', border: '1px solid #334155' }} /><Line type="monotone" dataKey="waterCm" name="Mực nước (cm)" stroke="#38bdf8" dot={false} strokeWidth={2} /><Line type="monotone" dataKey="rainMm" name="Mưa (mm)" stroke="#a78bfa" dot={false} strokeWidth={2} /></LineChart></ResponsiveContainer>}</div>; }
